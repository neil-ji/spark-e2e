# AESTHETICS.md — UI Review Aesthetic Principles

> 可积累、可进化的 UI review 审美习惯。
> 每次 review 时这些规则会自动注入到 VLM prompt 中。
> 修改此文件即可全局生效，提交到 git 则团队共享。

## Layout & Structure

- 追求视觉上的**对称**与**秩序感** — 元素应对齐到同一网格线，不要出现"差一点对齐"的偏移
- 同一页面内所有 section/卡片应保持**一致的间距**，内边距和外边距不应忽大忽小
- **全局一致性优先** — 同一类型的元素（按钮、卡片、标题）应使用统一的尺寸、颜色、字体样式
- 避免"孤岛布局" — 每个元素应与其周围元素形成明确的视觉关系（对齐、分组、间距节奏）

## Spacing & Proportion

- 间距应遵循**可感知的节奏** — 使用倍数关系（如 8px 基准），避免无规律的任意间距
- **留白不是浪费** — 适当的留白让 UI 呼吸，拥挤比稀疏更糟糕
- 内容区域与容器边缘之间应有**一致的安全距离**

## Color & Typography

- 色彩应有**明确的语义层级** — 主色、辅色、中性色各自扮演清晰角色，不混用
- 同一个含义用同一个颜色 — 不要`绿色`表示"成功"又表示"已读"
- 字号层级应形成**清晰的阶梯**（如 H1/H2/H3/body/caption），不要出现两个几乎相同但不完全相同的字号

## Visual Balance

- 页面整体应呈现**协调的重量分布** — 不要一侧很重（大图、深色块）另一侧很轻（空白、小字）
- **不要把页面塞满** — 内容密度应有呼吸感，信息层级清晰可辨
- 图标、插图、图表应和文字内容形成**和谐的比例关系**

## How to Add

在此文件中以 markdown 列表形式添加你的审美规则。
每次 `spark-e2e review` 命令会自动读取并注入到 VLM 分析上下文中。

示例：
```markdown
## My Rules

- 所有表单的 label 必须左对齐，输入框宽度统一
- 数据看板中同类指标卡必须等高等宽
- 移动端按钮最小点击区域 44×44px
```
